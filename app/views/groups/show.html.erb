<p id="notice"><%= notice %></p>

<p>
  <strong>Group:</strong>
  <%= @group.group_id %>
</p>

<p>
  <strong>Name:</strong>
  <%= @group.name %>
</p>

<p>
  <strong>Creator:</strong>
  <%= @group.creator_id %>
</p>

<%= link_to 'Show Pad list', group_pads_path(@group) %> |
<%= link_to 'Create Pad', new_group_pad_path(@group) %> |
<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
